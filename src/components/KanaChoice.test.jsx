import React from 'react';
import {render} from '@testing-library/react';
import {KanaChoice} from './KanaChoice';

describe('KanaChoice', () => {
  it('renders', () => {
    render(<KanaChoice />);
  });
});
